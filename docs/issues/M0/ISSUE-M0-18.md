# ISSUE-M0-18: UI 状态枚举规范

## Parent
PRD-100 §11.4.1

## What to build
定义统一 UI 状态枚举规范：加载中（骨架屏/loading）、空状态（插图+引导）、失败（红色提示+重试）、禁用（灰色+tooltip）。补充操作反馈规范和边界条件规范。

## Acceptance criteria
- 覆盖 PRD-100 §11.4 全部规范
- 每种 UI 状态有明确的视觉和交互规格
- 边界条件有完整处理策略
- 可直接作为前端组件开发输入

## Blocked by
None — 可立即开始
